
#include <vector>
#include "../model/Chamber.h"
using namespace std;

std::vector<Chamber> charArrayToChamber(std::vector<std::vector<char>> charArray){
    int counter = 0;
    for (int h = 0; h < charArray.size(); h++){
        for (int i = 0; i < charArray[h].size(); i++){
            if (charArray[h][i] == '.'){
                
            }
        }
    }
}
